# Archived Pokellection logos (Pokéball mark)

Previous header assets kept for optional reuse:

- `pokellection-logo-mark.png` — light mode
- `pokellection-logo-mark-dark.png` — dark mode
- `pokellection-logo.png` — full-size source

Active logos live in `/public/brand/` (fanned cards + wordmark SVG).

To switch back to these in the app, update `src/components/SiteLogo.tsx` to use the PNG paths under `/brand/archive/` (see git history for the prior `Image` implementation).
